# Supabase Configuration
# Get these values from your Supabase project dashboard:
# https://app.supabase.com/project/_/settings/api

# Supabase Project URL
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous/Public Key (safe to expose in client)
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Google Places API Configuration
# Get your API key from Google Cloud Console:
# https://console.cloud.google.com/apis/credentials
#
# Setup steps:
# 1. Create a new Google Cloud project (or use existing)
# 2. Enable the "Places API" and "Places API (New)" in APIs & Services
# 3. Create an API key in Credentials
# 4. Restrict the API key:
#    - Application restrictions: Set to your app's bundle ID (iOS) and package name (Android)
#    - API restrictions: Restrict to Places API only
# 5. For production, also enable billing (free tier: $200/month credit)

EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=your-google-places-api-key-here

# Note: Never commit your actual .env file to version control
# Copy this file to .env and fill in your actual values
